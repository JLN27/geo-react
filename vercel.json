{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "devCommand": "vite",
        "buildCommand": "vite build"
      }
    }
  ]
}